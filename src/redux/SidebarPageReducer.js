let initialState = {
    friends: [
        { name: "Vlados" },
        { name: "Alex" },
        { name: "Viktor" },
    ]
};

const SidebarPageReducer = (state = initialState, action) => {
    return state;
};

export default SidebarPageReducer;